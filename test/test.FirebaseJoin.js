
var assert = require('chai').assert;

describe('FirebaseJoin', function() {
   describe('test assert', function() {
      it('should run', function() {
         assert.ok(true, 'is it okay?');
      })
   });
});
